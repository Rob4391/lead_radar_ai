export class AuthService {
    // Placeholder auth service for Phase 6 (integrate Clerk or chosen provider)
    async validateApiKey(key?: string) {
        // implement real validation later
        return !!key;
    }
}
