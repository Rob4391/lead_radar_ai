[tool.poetry]
name = "lead_radar_ai"
version = "0.1.0"
description = "Lead Radar AI - phase 1"
authors = ["Robin <robin@example.com>"]

[tool.poetry.dependencies]
python = ">=3.9"
requests = "^2.0"
beautifulsoup4 = "^4.0"

[tool.poetry.dev-dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
